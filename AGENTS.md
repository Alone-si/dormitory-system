# AGENTS.md

## 目标与优先级

这是个人开发项目。以可运行、可维护和低成本为目标，优先级依次为：正确性与数据安全、简单性、可维护性、交付速度。

- 先理解目标和现有代码，再修改。
- 事实、判断和假设要分清；不确定时明确说明。
- 采用能可靠解决问题的最小改动，避免顺手扩展范围。

## 工作方式

1. 先读取与任务直接相关的文件、调用方和项目说明。
2. 只有关键缺失信息会明显改变结果时才提问；否则采用合理假设并说明。
3. 简单任务直接完成；复杂任务先给不超过 3 步的短计划。
4. 修改后先运行最小且相关的检查，失败后再扩大诊断范围。
5. 持续处理到请求完成或遇到无法自行解决的真实阻塞。
6. 最后说明改了什么、如何验证、剩余风险；不要重复过程日志。

## 简洁与 Token 控制

- 使用 `rg` 定位文件和引用，只读取当前任务需要的内容。
- 不重复搜索已经确认的信息，不重复总结相同结论。
- 默认不用子代理；只有两个以上独立任务能明显并行提速时才使用。
- 不为简单任务生成长计划、架构图、规格文档或额外报告。
- 不罗列大量可选方案；给出当前最合适的方案和关键理由。
- 不写与实现一一对应、没有额外验证价值的测试。
- 完成必要验证后停止，不重复运行无意义的检查。
- 输出保持简洁；用户要求教程、报告或详细分析时再展开。

## 编码原则

- 遵循项目现有结构、命名、格式和工具链。
- 优先复用现有代码、标准库和已安装依赖。
- 遵循 YAGNI：不添加尚未出现的扩展点、抽象层或配置项。
- 不为单一实现创建接口、工厂、事件总线或复杂分层。
- 函数和模块职责清楚；仅在说明“为什么”有价值时写注释。
- 修复根因，并检查共享路径的其他调用方，避免只遮盖症状。
- 保持兼容性；需要破坏性变更时明确说明影响和迁移方式。
- 不擅自引入 JWT、Redis、消息队列、微服务或复杂账户系统。

## 测试与验收

- 测试投入与风险成比例。
- 文案、样式和低风险配置修改：运行构建、静态检查或最小烟雾测试即可。
- 分支逻辑、循环、解析器、金额、安全、权限和数据变更：保留至少一个能捕获回归的有效测试。
- 优先测试公开行为和真实接口，避免测试私有实现细节。
- 不因为已有无关失败而修改任务范围；准确报告失败及其关系。
- 未实际运行的检查不得写成“已通过”。

## 依赖与安全

- 添加依赖前先确认标准库、平台能力或现有依赖不能满足需求。
- 只添加直接需要的依赖，并说明用途；避免大型框架解决小问题。
- 不把密码、令牌、私钥、连接串或个人敏感信息写进代码和 Git。
- 使用环境变量和示例占位符；日志与错误信息不得泄露密钥。
- 对外部输入做必要校验，对删除、覆盖、迁移和不可逆操作确认精确范围。
- 未经明确要求，不部署、不发布、不推送远端、不发送外部消息。

## 文件与 Git

- 保留用户已有修改，不覆盖无关文件。
- 禁止使用 `git reset --hard`、强制推送或大范围清理来解决局部问题。
- 不提交生成物、缓存、密钥或机器专属配置。
- 除非用户明确要求，否则不创建提交、分支或 Pull Request。

## 完成标准

只有同时满足以下条件才算完成：

- 请求的行为已经实现。
- 相关检查通过，或已清楚说明无法通过的原因。
- 没有引入与目标无关的功能和依赖。
- 用户能从最终回复中直接找到结果、验证方式和重要限制。

# 换电脑运行指南

1. 安装 Java 17、Node.js 24、MySQL 8 和 Git。
2. 克隆仓库，不要复制 `node_modules`、`dist` 或 `target`。
3. 如需保留原数据，用 `mysqldump` 在旧电脑备份，并在新电脑导入。数据库备份包含业务数据，请妥善保管。
4. 复制 `backend/config/application-local.example.properties` 为 `backend/config/application-local.properties`，填写新电脑的 MySQL 密码。
5. 在 `frontend` 目录执行 `npm ci`，然后按 [README-启动说明.md](README-启动说明.md) 启动项目。
6. 如果是空数据库，按 [README.md](README.md) 的“本地启动”章节设置首个管理员环境变量。

完整配置、构建和故障排查以 [README.md](README.md) 为准。
# 换电脑运行指南 - 100%能跑起来

## 📦 第一步：打包旧电脑的项目

### 1. 备份数据库
```bash
# 方法一：使用Navicat（推荐）
# 1. 打开Navicat
# 2. 右键数据库 "alone" -> 转储SQL文件 -> 结构和数据
# 3. 保存为 alone_backup.sql

# 方法二：使用命令行
mysqldump -u root -p alone > alone_backup.sql
```

### 2. 打包项目文件
```bash
# 直接压缩整个项目文件夹
# 右键 SmartDormSystem 文件夹 -> 发送到 -> 压缩文件
# 或使用命令：
zip -r SmartDormSystem.zip SmartDormSystem/
```

**重要提示**：
- ✅ 整个项目文件夹都要打包（包括前端和后端）
- ✅ 数据库备份文件单独保存
- ❌ 不需要打包 `node_modules` 和 `target` 文件夹（可以删除后再打包，减小体积）

---

## 🖥️ 第二步：新电脑环境准备

### 必须安装的软件（缺一不可）

#### 1. JDK 17
```bash
# 下载地址：https://www.oracle.com/java/technologies/downloads/#java17
# 或使用：https://adoptium.net/

# 安装后验证：
java -version
# 应该显示：java version "17.x.x"
```

#### 2. Node.js 18+
```bash
# 下载地址：https://nodejs.org/
# 选择 LTS 版本

# 安装后验证：
node -v
# 应该显示：v18.x.x 或更高

npm -v
# 应该显示：9.x.x 或更高
```

#### 3. MySQL 8.0
```bash
# 下载地址：https://dev.mysql.com/downloads/mysql/
# 或使用：https://www.mysql.com/downloads/

# 安装后验证：
mysql --version
# 应该显示：mysql Ver 8.0.x
```

#### 4. Maven（可选，推荐）
```bash
# 下载地址：https://maven.apache.org/download.cgi

# 安装后验证：
mvn -v
# 应该显示：Apache Maven 3.x.x
```

**如果没有Maven**：项目自带 `mvnw`（Maven Wrapper），可以不用单独安装

---

## 🚀 第三步：新电脑部署步骤

### 1. 解压项目文件
```bash
# 解压到任意位置，例如：
D:\Projects\SmartDormSystem
```

### 2. 恢复数据库

#### 方法一：使用Navicat（最简单）
```
1. 打开Navicat
2. 创建连接（localhost, root, 你的密码）
3. 新建数据库：alone
   - 字符集：utf8mb4
   - 排序规则：utf8mb4_unicode_ci
4. 右键数据库 -> 运行SQL文件 -> 选择 alone_backup.sql
5. 等待导入完成
```

#### 方法二：使用命令行
```bash
# 1. 登录MySQL
mysql -u root -p

# 2. 创建数据库
CREATE DATABASE alone CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
exit;

# 3. 导入数据
mysql -u root -p alone < alone_backup.sql
```

### 3. 修改后端配置

**复制文件**：将 `backend/config/application-local.example.properties` 复制为 `backend/config/application-local.properties`

**必须修改的配置**：
```properties
# 填写新电脑的MySQL密码（本机文件不会提交）
spring.datasource.password=你的新密码

# 其他配置一般不需要改
spring.datasource.url=jdbc:mysql://localhost:3306/alone?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=root
```

### 4. 启动后端

#### 方法一：使用命令行（推荐）
```bash
# 进入后端目录
cd D:\Projects\SmartDormSystem\backend

# 如果有Maven
mvn clean install
mvn spring-boot:run

# 如果没有Maven，使用项目自带的mvnw
.\mvnw.cmd clean install
.\mvnw.cmd spring-boot:run
```

#### 方法二：使用IDEA
```
1. 打开IDEA
2. File -> Open -> 选择 backend 文件夹
3. 等待Maven依赖下载完成
4. 找到 BwxwApplication.java
5. 右键 -> Run 'BwxwApplication'
```

**看到这个说明后端启动成功**：
```
Started BwxwApplication in X.XXX seconds
```

**访问测试**：打开浏览器访问 `http://localhost:8080`
- 如果看到 "Whitelabel Error Page" 说明后端正常运行

### 5. 启动前端

```bash
# 进入前端目录
cd D:\Projects\SmartDormSystem\frontend

# 安装依赖（第一次必须执行）
npm install

# 启动开发服务器
npm run dev
```

**看到这个说明前端启动成功**：
```
VITE v5.x.x  ready in XXX ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

**访问测试**：打开浏览器访问 `http://localhost:5173`

---

## ✅ 第四步：验证系统运行

### 测试登录
1. 打开浏览器访问：`http://localhost:5173`
2. 使用测试账号登录：
   - **管理员**：admin / 123456
   - **学生**：20240001 / 123456

### 如果能登录成功，说明系统完全正常！

---

## ❌ 常见问题和解决方案

### 问题1：后端启动失败 - 端口被占用
```
Error: Port 8080 is already in use
```

**解决方案**：
```properties
# 修改 application.properties
server.port=8081

# 同时修改前端的API地址
# frontend/src/utils/request.js
baseURL: 'http://localhost:8081/api'
```

### 问题2：后端启动失败 - 数据库连接失败
```
Error: Access denied for user 'root'@'localhost'
```

**解决方案**：
1. 检查MySQL是否启动
2. 检查 `backend/config/application-local.properties` 中的密码是否正确
3. 尝试用命令行登录MySQL验证：`mysql -u root -p`

### 问题3：前端启动失败 - npm install 报错
```
Error: ENOENT: no such file or directory
```

**解决方案**：
```bash
# 清除缓存重新安装
npm cache clean --force
npm install
```

### 问题4：前端能访问但无法登录
```
Network Error 或 404
```

**解决方案**：
1. 检查后端是否启动（访问 http://localhost:8080）
2. 检查前端API地址是否正确（`frontend/src/utils/request.js`）
3. 检查浏览器控制台的错误信息（F12）

### 问题5：Maven依赖下载慢
**解决方案**：配置国内镜像

创建或修改 `C:\Users\你的用户名\.m2\settings.xml`：
```xml
<settings>
  <mirrors>
    <mirror>
      <id>aliyun</id>
      <mirrorOf>central</mirrorOf>
      <name>Aliyun Maven</name>
      <url>https://maven.aliyun.com/repository/public</url>
    </mirror>
  </mirrors>
</settings>
```

### 问题6：npm install 下载慢
**解决方案**：使用淘宝镜像
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

---

## 📋 快速检查清单

在新电脑上按顺序检查：

- [ ] **环境检查**
  - [ ] `java -version` 显示 17.x.x
  - [ ] `node -v` 显示 18.x.x 或更高
  - [ ] `mysql --version` 显示 8.0.x
  - [ ] MySQL服务正在运行

- [ ] **项目文件**
  - [ ] 项目文件已解压
  - [ ] 数据库备份文件已准备好

- [ ] **数据库**
  - [ ] 数据库 `alone` 已创建
  - [ ] 数据已导入
  - [ ] 能用Navicat连接并看到数据

- [ ] **后端配置**
  - [ ] `backend/config/application-local.properties` 中的密码已填写
  - [ ] 端口8080未被占用

- [ ] **后端启动**
  - [ ] 执行 `mvn spring-boot:run` 或 `.\mvnw.cmd spring-boot:run`
  - [ ] 看到 "Started BwxwApplication"
  - [ ] 访问 http://localhost:8080 有响应

- [ ] **前端启动**
  - [ ] 执行 `npm install`（第一次）
  - [ ] 执行 `npm run dev`
  - [ ] 看到 "Local: http://localhost:5173/"
  - [ ] 访问 http://localhost:5173 能看到登录页

- [ ] **功能测试**
  - [ ] 能用 admin/123456 登录
  - [ ] 能看到管理员后台
  - [ ] 能看到数据（学生、宿舍等）

---

## 🎯 保证100%能运行的关键点

1. **环境版本要对**
   - JDK 必须是 17
   - Node.js 必须是 18+
   - MySQL 必须是 8.0+

2. **数据库必须导入成功**
   - 用Navicat打开能看到所有表
   - 能看到数据（users、rooms等表有数据）

3. **配置文件必须改对**
   - 数据库密码必须是新电脑的密码
   - 端口不能被占用

4. **依赖必须下载完整**
   - Maven依赖下载完成（看到BUILD SUCCESS）
   - npm依赖下载完成（看到node_modules文件夹）

5. **启动顺序**
   - 先启动后端（等看到Started）
   - 再启动前端（等看到Local地址）

---

## 💡 额外建议

### 使用Docker（高级，可选）
如果经常换电脑，可以考虑使用Docker：
- 一次配置，到处运行
- 不用担心环境问题
- 数据库也可以容器化

### 使用云服务器（推荐）
- 部署到阿里云/腾讯云
- 换电脑只需要访问网址
- 数据永久保存

### 定期备份
- 每周备份一次数据库
- 保存到云盘（百度网盘/OneDrive）
- 项目代码用Git管理

---

## 📞 遇到问题怎么办？

1. **查看错误信息**
   - 后端：看命令行的红色错误
   - 前端：按F12看浏览器控制台

2. **检查端口**
   - 后端：8080
   - 前端：5173
   - 数据库：3306

3. **重启大法**
   - 关闭所有命令行窗口
   - 重启MySQL服务
   - 重新启动后端和前端

4. **最后的办法**
   - 删除 `backend/target` 文件夹
   - 删除 `frontend/node_modules` 文件夹
   - 重新执行安装和启动命令

---

## ✨ 总结

**只要按照这个文档一步步来，保证能在新电脑上运行起来！**

**最重要的三点**：
1. ✅ 环境安装对（JDK 17 + Node.js 18+ + MySQL 8.0）
2. ✅ 数据库导入成功
3. ✅ 配置文件改对（数据库密码）

**其他都是自动的，不用担心！**
